#include "main.h"
/**
* _islower - this if lowercase characters are there
* @c: this is the character check
* Return: 1 if c is lowercase, 0 otherwise
*/
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
