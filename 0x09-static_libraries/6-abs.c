#include "main.h"
/**
 * _abs - This method runs an gets absolute value of an integer
 * @n:  the only int to check
 * Return:  returns abslvalue of int
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
