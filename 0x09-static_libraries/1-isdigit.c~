#include "main.h"
/**
* _isdigit - this checks the digits from (0 through 9)
* @c: integers to check
* Return: true 1 if c its a digit, 0 otherwise
*/
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
